import E0PublicLayout from '@/components/landing/E0PublicLayout';

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <E0PublicLayout>{children}</E0PublicLayout>;
}
