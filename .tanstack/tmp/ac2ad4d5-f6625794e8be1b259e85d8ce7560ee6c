import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/api/admin/auth/refresh')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/api/admin/auth/refresh"!</div>
}
